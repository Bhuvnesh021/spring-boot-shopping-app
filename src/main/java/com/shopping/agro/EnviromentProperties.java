package com.shopping.agro;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.PropertySource;
import org.springframework.core.env.Environment;
public class EnviromentProperties {
   /* private static EnviromentProperties enviromentProperties = new EnviromentProperties();
    @Autowired
    private Environment env;

    private EnviromentProperties(){

    }
    public static EnviromentProperties getInstance() {
        return enviromentProperties;
    }

    public String getServerPort(){
        return env.getProperty("server.port");
    }*/
}
